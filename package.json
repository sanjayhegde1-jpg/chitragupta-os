{
  "name": "chitragupta-os",
  "version": "1.0.0",
  "description": "Enterprise AI OS for Manufacturing CRM and Social Media Automation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preflight": "node scripts/preflight-node.cjs",
    "lint": "npm run lint --workspace web",
    "typecheck": "node scripts/preflight-node.cjs && npx tsc -p apps/web/tsconfig.json --noEmit && npx tsc -p packages/functions/tsconfig.json --noEmit && npx tsc -p packages/shared/tsconfig.json --noEmit",
    "test": "playwright test",
    "build": "npm run build --workspaces --if-present"
  },
  "keywords": [],
  "author": "Chitragupta Engineering Taskforce",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "ts-node": "^10.9.2"
  },
  "overrides": {
    "sharp": "0.33.5"
  }
}
